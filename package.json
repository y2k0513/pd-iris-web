{
  "name": "pd-iris-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "모바일 웹 카메라와 MediaPipe Face Landmarker를 이용한 근사 PD 측정 데모",
  "scripts": {
    "postinstall": "node scripts/copy-wasm.mjs",
    "setup:model": "node scripts/download-model.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35"
  },
  "devDependencies": {
    "vite": "7.3.6"
  },
  "engines": {
    "node": ">=20"
  }
}
